# robertomanfreda.github.io

Personal GitHub Pages site for Roberto Manfreda.

The site is intentionally static: `index.html`, `styles.css`, `app.js`, `robots.txt`, and `sitemap.xml` are enough for GitHub Pages.
